# WiFi Server

Generic Wi-Fi TCP server code for the Raspberry Pi Pico W.

## Folder Structure

```
WiFi_Server/
└── wifi_tcp_server.py   # Connect to Wi-Fi, listen for TCP connections, print received messages
```
